import { View, Text } from 'react-native'
import React from 'react'
import { colors } from '../../utils/constants/colors'

const MainScreen = () => {
  return (
    <View style={{flex: 1, backgroundColor: colors.red1}}>
      <Text>index</Text>
    </View>
  )
}

export default MainScreen